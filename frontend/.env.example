# Optional frontend-local, server-only overrides belong in frontend/.env.local.
# Shared BFF credentials and the operator password remain in the root .env and
# are loaded internally by next.config.mjs. Never use NEXT_PUBLIC_ for them.
BACKEND_INTERNAL_URL=http://127.0.0.1:22222
