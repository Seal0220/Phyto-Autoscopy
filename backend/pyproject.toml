[project]
name = "phyto-autoscopy"
version = "0.1.0"
description = "API-only FastAPI hardware backend for the CHLOROCULUS control system."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "websockets",
  "opencv-python",
  "numpy",
  "Phidget22",
  "pydantic",
  "sqlalchemy",
]

[project.optional-dependencies]
test = ["pytest", "httpx"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
